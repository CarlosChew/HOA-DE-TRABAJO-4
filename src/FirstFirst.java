abstract class FirstFirst implements Namer { //split first last
    public FirstFirst(String s) {
       int i = s.lastIndexOf(" "); //find sep space
       if (i > 0) {
          
       }
       else {
        
       }
    }
}